/**
 * Reverses the order of items in `in_a` by mutating `in_a`.
 */
export function reverse(in_a) {
    return in_a.reverse();
}
export function maybe_reverse(maybe_a) {
    return maybe_a ? reverse(maybe_a) : void 0;
}
//# sourceMappingURL=src/reverse.js.map