/**
 * Returns a function that returns from [every](#every) with the given `predicate` function.
 */
export declare function _every<I extends unknown = unknown>(predicate: (item: I, number: any, a1: I[]) => boolean): (a1: I[]) => boolean;
export { _every as _every_fn, _every as _fn__every, };
