import type { maybe } from '@ctx-core/function';
/**
 * Returns Array of inverse values (1/n) of each item in `a1`.
 */
export declare function map_inverse(a1: maybe<number[]>): number[];
export declare const map__inverse: typeof map_inverse;
